import TaskList from './TaskList'
import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'

<Meta title="tasks/render list of tasks" />

Import the component in your file:  
`import { AddTask } from 'modules/tasks/taskList';`

### Empty tasks

<Preview>
  <Story name="no tasks available">
    <TaskList tasks={[]} removeTask={action('click')} />
  </Story>
</Preview>

### Tasks added

<Preview>
  <Story name="tasks added">
    <TaskList
      tasks={[
        {
          _id: 'task-1',
          title: 'Learning react testing library',
          completed: false,
          createdAt: new Date(),
        },
        {
          _id: 'task-2',
          title: 'Learning mocha and chai',
          completed: false,
          createdAt: new Date(),
        },
      ]}
      removeTask={action('click')}
    />
  </Story>
</Preview>

<Props of={TaskList} />
