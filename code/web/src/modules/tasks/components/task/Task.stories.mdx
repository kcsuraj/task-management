import Task from './Task'
import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'

<Meta title="tasks/task" />

Import the component in your file:  
`import { Task } from 'modules/tasks/Task';`

### Incomplete task

<Preview>
  <Story name="incomplete task">
    <Task
      task={{
        _id: 'task-1',
        title: 'Learning react testing library',
        completed: false,
        createdAt: new Date(),
      }}
      removeTask={action('click')}
    />
  </Story>
</Preview>

### Complete task

<Preview>
  <Story name="complete task">
    <Task
      task={{
        id: 'task-1',
        task: 'Learning react testing library',
        completed: true,
      }}
      removeTask={action('click')}
    />
  </Story>
</Preview>

<Props of={Task} />
